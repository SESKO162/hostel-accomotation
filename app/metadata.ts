import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Hostel Management System',
  description: 'A modern hostel management system with fingerprint authentication',
};

export default metadata;
